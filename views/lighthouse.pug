extends layout

block content
  h1= title

  - let items = results
  
  div(class="container-fluid")
    div(class="row")
      each item, index in results 
        div(class="col-sm-2", onclick=`buy(${index})`)
          h3 #{item.name}
          p Location: #{item.location}
          p Height: #{item.height}ft

  script.
    function buy(index) {
      alert(`You purchased item ${index}`);
    }